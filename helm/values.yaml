# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: yashmadon
  tag: "v1.1.1-6f21dff-amd64"
  arch: amd64

secret:
  name: "new-poject"
  env: "TELE_TOKEN"
  key: "token"
